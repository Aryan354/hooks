import {createTheme} from "@mui/material";

export const theme = createTheme({
    palette: {
        primary:{
            main: "green",
            light: "yellow"
        },
        secondary:{
            main: "skyblue",
            light: "violet"
        }
    }
})